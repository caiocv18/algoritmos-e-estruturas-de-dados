# Cálculo de média simples
numeros = [23, 45, 67, 89, 12]
soma = sum(numeros)
quantidade = len(numeros)
media = soma / quantidade

# Encontrar valores extremos
minimo = min(numeros)
maximo = max(numeros)

# Operações com conjuntos para encontrar elementos comuns
lista1 = [1, 2, 3, 4, 5]
lista2 = [3, 4, 5, 6, 7]
comuns = set(lista1).intersection(set(lista2))